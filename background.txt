你现在要协助我完成硕士论文第四章的“风险因果链最小版本”知识图谱推断实验。

任务背景：
- 这是一个面向新诉求节点的归纳式关系推断任务。
- 只保留 6 类节点：诉求、实体、隐患、风险、事件、后果。
- 只保留 9 类关系：
  1. 诉求-包含实体-实体
  2. 诉求-包含隐患-隐患
  3. 诉求-包含事件-事件
  4. 诉求-包含风险-风险
  5. 诉求-包含后果-后果
  6. 实体-易感于-隐患
  7. 隐患-导致-风险
  8. 事件-触发风险-风险
  9. 风险-导致-后果

实验目标：
- 只做两个预测任务：
  1. {诉求, 包含风险, ?}
  2. {诉求, 包含后果, ?}

数据划分要求：
- 必须按 complaint_id 做 train / valid / test 划分，不能按三元组随机切分。
- valid/test 中的诉求节点必须是训练未见的新诉求节点。
- 测试时对新诉求只保留：
  - 包含实体
  - 包含隐患
  - 包含事件
- 将：
  - 包含风险
  - 包含后果
  作为待预测目标，写入 queries 文件。

当前目标：
- 我已经完成了两个基线：TransE 和 R-GCN 的最小版本。
- 接下来要继续优化基线，并实现我自己的最小模型。
- 你的任务是按我每一步要求，逐步修改代码，不要一次性重构整个项目。

工作要求：
1. 每次先阅读相关代码并复述你的理解。
2. 明确指出当前实现的问题和修改点。
3. 只改我要求的文件，尽量保持项目结构不变。
4. 输出完整可运行代码，不要只给伪代码。
5. 每次修改后说明：
   - 改了哪些文件
   - 改了什么
   - 为什么这样改
   - 我该如何验证
6. 如果你发现我的实验设定和当前代码冲突，要明确指出，不要自作主张更改任务定义。